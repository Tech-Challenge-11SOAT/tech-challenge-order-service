apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-order-service 
  namespace: techchallenge
  labels:
    app: order-service 
spec:
  selector:
    app: order-service 
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: LoadBalancer